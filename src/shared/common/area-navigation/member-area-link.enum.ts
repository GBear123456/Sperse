export class MemberAreaLink {
    namePrefix?: string;
    name: string;
    routerUrl?: string;
    imgUrl?: string;
    activeImgUrl?: string;
    sublinks?: MemberAreaLink[];
    hidden?: boolean;
}
